let n = 5;
let asteriskPyramid;

if (n > 1) {
  for (indexLine = 0; indexLine < n; indexLine += 1) {
    for (indexColumn = 0; indexColumn < n; indexColumn += 1) {
      if (indexLine % 2 === 0) {
        //linha 0 tem 1
        //linha 2 tem 3
        //linha 4 tem 5
        
      }
    }
  }
}